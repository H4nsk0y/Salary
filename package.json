{
  "name": "alvisa-salary",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.cjs -i ./styles/tailwind-input.css -o ./styles/tailwind.css --minify",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build:css && npm test"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
